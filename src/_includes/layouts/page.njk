---
layout: layouts/base.njk
---
<div class="prose">
{{ content | safe }}
</div>

{%- if season_url %}{% include "partials/season-link.njk" %}{% endif %}

{#- `data_section` in a page's front matter pulls in a table built from a file
    in _data/. It is a plain word, not a path; see CONTRIBUTING-DEV.md. -#}
{%- if data_section == "club-directory" %}{% include "partials/club-directory.njk" %}{% endif %}
{%- if data_section == "field-list" %}{% include "partials/field-list.njk" %}{% endif %}
{%- if data_section == "uniform-table" %}{% include "partials/uniform-table.njk" %}{% endif %}
